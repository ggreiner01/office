<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :orderid %>
    <%= form.number_field :orderid, id: :order_orderid %>
  </div>

  <div class="field">
    <%= form.label :supply_id %>
    <%= form.number_field :supply_id, id: :order_supply_id %>
  </div>

  <div class="field">
    <%= form.label :employee_id %>
    <%= form.number_field :employee_id, id: :order_employee_id %>
  </div>

  <div class="field">
    <%= form.label :supplier_id %>
    <%= form.number_field :supplier_id, id: :order_supplier_id %>
  </div>

  <div class="field">
    <%= form.label :totalcost %>
    <%= form.text_field :totalcost, id: :order_totalcost %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.text_field :quantity, id: :order_quantity %>
  </div>


  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date, id: :order_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
