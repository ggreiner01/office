<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <h1 class="page-header">Inventory
        <small>Stock & Orders</small>
      </h1>
      <ol class="breadcrumb">
        <li>
          <a class="text-muted" href="/">Home</a>
        </li> ||
        <li class="active">Inventory</li>
      </ol>
    </div>
  </div>
  <div class="row">
    <div class="col-md-12">
      <h2>Current Inventory</h2>
      <table class="table table-hover">
        <thead>
          <tr>
            <th>Supply ID</th>
            <th>Supply Name</th>
            <th>Quantity</th>
            <% if current_user.privileges? %>
              <th>Order</th>
            <% end %>
          </tr>
        </thead>

        <tbody>

          <%
               @supplyQuantity.each do |t| %>
            <tr>
              <td><%= t.supply_id %></td>
              <td><%= t.supplyname %></td>
              <td><%= t.quantity %></td>
              <% if current_user.privileges? %>
                <td>
                  <%= form_for @order_item do |f| %>
                  <%= f.hidden_field :supply_id, value: t.suppliesid %>
                  <%= f.number_field :quantity %>
                  <%= f.submit "Order More", class: "btn-xs btn-success" %>
                <% end %>
              </td>
            <% end %>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Supply ID</th>
      <th>Supplier</th>
      <th>Supply</th>
      <th>Cost</th>
    </tr>
  </thead>

  <tbody>
    <%
       @oInfo.each do |t| %>
      <tr>
        <td><%= t.supply_id %></td>
        <td><%= t.name %></td>
        <td><%= t.supplyname %></td>
        <td><%= t.cost %></td>
        <td></td>
      </tr>
    <% end %>
  </tbody>
</table>

<!-- /.row -->

<!-- Team Members -->
<div class="row">
  <div class="col-lg-12">
    <h2 class="page-header">Recent Orders</h2>
  </div>

  <% @osInfo.each do |os| %>
    <div class="col-md-4 text-center">
      <div class="card text-white bg-secondary mb-3" style="max-width: 18rem;">
        <div class="card-header"><%= os.supplyname %></div>
        <div class="card-body">
          <h5 class="card-title"><%= os.first_name + ' ' + os.last_name %></h5>
          <p class="card-text"><%= os.quantity %>
            <%= os.supplyname %>
            from
            <%= os.name %>
            totaling $<%=os.totalcost %>0 on
            <%= os.created_at %>.</p>
        </div>
      </div>
    </div>
  <% end %>

</div>
<!-- /.row -->
<hr>

</div>
