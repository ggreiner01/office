<%= form_with(model: supply, local: true) do |form| %>
  <% if supply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(supply.errors.count, "error") %> prohibited this supply from being saved:</h2>

      <ul>
      <% supply.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :suppliesid %>
    <%= form.number_field :suppliesid, id: :supply_suppliesid %>
  </div>

  <div class="field">
    <%= form.label :supplyname %>
    <%= form.text_field :supplyname, id: :supply_supplyname %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
