<%= form_with(model: order_status, local: true) do |form| %>
  <% if order_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_status.errors.count, "error") %> prohibited this order_status from being saved:</h2>

      <ul>
      <% order_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :orderstatusid %>
    <%= form.number_field :orderstatusid, id: :order_status_orderstatusid %>
  </div>

  <div class="field">
    <%= form.label :orderid %>
    <%= form.number_field :orderid, id: :order_status_orderid %>
  </div>

  <div class="field">
    <%= form.label :complete %>
    <%= form.number_field :complete, id: :order_status_complete %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
